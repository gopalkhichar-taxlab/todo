/**
 * Re-exports the ListTasksQuery schema from the shared @kudo/schemas package.
 * Kept as a local file so internal imports stay consistent.
 */
export { ListTasksQuerySchema, type ListTasksQuery } from '@kudo/schemas';
