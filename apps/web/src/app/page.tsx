/**
 * Placeholder landing page. The login (TAL-79 frontend) and dashboard
 * (TAL-87+) stories replace this with the real UX.
 */
export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center gap-6 p-8 text-center">
      <h1 className="text-4xl font-bold tracking-tight">KUDO</h1>
      <p className="text-lg text-gray-600">
        Strategic task management. Tie every task to an objective that matters.
      </p>
      <p className="text-sm text-gray-500">
        Scaffolding only — the real UI lands as feature stories merge.
      </p>
    </main>
  );
}
